<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/login.css" />
  <link rel="shortcut icon" href="/images/logo_login.png" type="image/x-icon" />
</head>
<body>
  <div class="login-container">
    <div class="login-card">
      <div class="logo-container">
        <img src="/images/logo_login.png" alt="Adelante Sumercé Logo" class="logo" />
      </div>

      <h1 class="login-title">Recuperar Contraseña</h1>
      <p class="login-subtitle">Ingrese su correo para recibir un enlace de recuperación</p>

      <% if (error) { %>
        <div class="error-message"><%= error %></div>
      <% } %>

      <form action="/forgot-password" method="POST" class="login-form">
        <div class="form-group">
          <input
            type="email"
            id="email"
            name="email"
            class="form-input"
            placeholder="Correo electrónico"
            required
            autocomplete="email"
          />
        </div>

        <button type="submit" class="btn-login">Enviar Enlace</button>
      </form>

      <div class="register-link-container">
        <p class="register-text">
          <a href="/login" class="register-link">Volver al inicio de sesión</a>
        </p>
      </div>
    </div>
  </div>
</body>
</html>
